<기본>

1. REMIX IDE에서 배포하고자하는 스마트컨트랙트 코드를 컴파일한다.
2. 컴파일 후 ABI를 복사하여 abi 폴더에 해당 컨트랙트.json 파일을 생성하여 ABI를 기록한다.
2. 프라이빗 네트워크로 연결하여 배포한다.
3. 배포(Deploy) 후 거래 주소를 복사하여 CartTableCompo.vue에서 contractAddress를 갱신해준다.

<수정>
- 새 코드로 컴파일 할 시 : ABI, contractAddress 모두 갱신.
- 코드가 같은데 배포만 새로 할 시 : contractAddress만 갱신.

*참고
-시연영상: https://youtu.be/eBMg99NJoc8
